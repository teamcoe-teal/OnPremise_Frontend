@model IEnumerable<PlanDigitization_web.Models.Toollist>
@{
    ViewBag.Title = "Tool_list";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<header class="page-header">
    <h2> Tool List Details </h2>
    <div class="right-wrapper pull-right">
        <ol class="breadcrumbs">
            <li>
                <a href="@Url.Action("MainDashboard","MainDashboard")">
                    <i class="fa fa-home"></i>
                </a>
            </li>
            <li><span>Settings</span></li>
            <li><span>Tools</span></li>
        </ol>
    </div>
</header>

<!-- Add New Loss Data-->
<div class="row" id="divnew" style="display:none;">
    <div class="col-md-12">
        <form class="form" action="@Url.Action("Insert_Toollist","UserSettings")" method="post" name="form1" id="submit2">
            <section class="panel">
                <header class="panel-heading" style="background-color:teal;color:white;">
                    <div class="panel-actions">
                    </div>
                    <h2 class="panel-title" style="color:white;">Add New Tool</h2>
                </header>
                <div class="panel-body">
                    <div class="form-group">
                        @*<label class="col-sm-2 control-label"> Line Code <span class="required">*</span></label>
                            <div class="col-sm-4">
                                <select class="form-control Line" name="Line_code" id="Line" title="Select Line" required></select>
                            </div>*@
                        <label class="col-sm-2 control-label">Machine Code <span class="required">*</span></label>
                        <div class="col-sm-4">

                            <select class="form-control Machine" name="Machine_Code" id="Machine" title="Select Machine" required>
                                <option value="">Select Line First</option>
                            </select>
                        </div>

                        <label class="col-sm-2 control-label"> Sub Assembly <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <select class="form-control Subassembly" name="Subassembly" id="Subassembly" title="Select Machine First" required>
                                <option value="">Select Machine</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label"> Tool ID <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="ToolID" name="ToolID" class="form-control .toolid" title="Enter ID" onchange="checktool()" placeholder="Tool ID" required />
                        </div>
                        <label class="col-sm-2 control-label"> Tool Name <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="ToolName" name="ToolName" class="form-control" title="Enter Name" placeholder="Name" required />
                        </div>
                    </div>
                    <div class="form-group">

                        @*<label class="col-sm-2 control-label"> Rated Life Cycle <span class="required">*</span></label>
                            <div class="col-sm-4">
                                <input type="number" id="RatedLifeCycle" name="RatedLifeCycle" class="form-control" title="Enter RatedLifeCycle" min="0" step="any" placeholder="Rated Life Cycle" required />
                            </div>*@
                        <label class="col-sm-2 control-label"> Part Number <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="PartNo" name="Part_number" class="form-control" title="Enter Part Number" placeholder="Part Number" required />
                        </div>
                    </div>
                    <div class="form-group">
                        @*<label class="col-sm-2 control-label"> Recommendation Text <span class="required">*</span></label>
                            <div class="col-sm-4">
                                <input type="text" id="Rectext" name="Rectext" class="form-control" title="Enter Recommendation Text" placeholder="Reco Text" required />
                            </div>*@
                        <label class="col-sm-2 control-label"> Serial Number <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="SerialNo" name="SerialNo" class="form-control" title="Enter Serial No" placeholder="Serial No" required />
                        </div>
                        <label class="col-sm-2 control-label"> Conversion Parameter <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="Conversion_parameter" onkeypress="return onlyNumberKey(event)" name="Conversion_parameter" class="form-control" title="Enter Parameter" placeholder="Parameter" required />
                        </div>

                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label"> Make <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="Make" name="Make" class="form-control" title="Enter Make" placeholder="Make" required />
                        </div>
                        <label class="col-sm-2 control-label"> UOM <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="UOM" name="UOM" class="form-control" title="Enter UOM" placeholder="UOM" required />
                        </div>
                    </div>
                    <div class="form-group">

                        <label class="col-sm-2 control-label"> Classification <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="Classification" name="Classification" class="form-control" title="Enter Classification" placeholder="Classification" required />
                        </div>
                    </div>
                    <br />
                    <table class="form-table" id="customFields">
                        <tr valign="top">
                            <th scope="row"><label for="customFieldName"></label></th>
                            <td>

                                <input type="number" class="code" id="RatedLifeCycle" name="RatedLifeCycle[]" min="0" oninput="validity.valid||(value='');" value="" placeholder="Rated Life Cycle" /> &nbsp; &nbsp; &nbsp; &nbsp;
                                <input type="text" class="code" id="Rectext" name="Rectext[]" value="" placeholder="Recommendation Text" /> &nbsp;


                                <a href="javascript:void(0);" class="addCF">Add</a>

                            </td>
                        </tr>
                    </table>
                </div>
                <footer class="panel-footer">
                    <div class="row">
                        <div class="col-sm-6 col-sm-offset-3" style="text-align:center;">
                            @*<button type="submit" class="btn btn-primary">Submit</button>*@
                            @*<input type="submit" value="Submit" />*@
                            <button type="submit" class="btn btn-primary submit">Submit</button>
                            <button type="reset" id="cancel" class="btn btn-danger cancel">Cancel</button>
                        </div>
                    </div>
                </footer>
            </section>
        </form>
    </div>
</div>

<!-- Operator Loss Details-->
<div class="row" id="divdetails">
    <div class="col-md-12">
        <section class="panel">
            <header class="panel-heading" style="background-color:teal;color:white;">

                <div class="panel-actions">
                    @if (ViewContext.Controller.HasPermission("ToolListSetting-Add"))
                    {
        <span style="color:black">Download CSV from gateway for Tool Info</span>
                        <button class="btn btn-success" id="add"><i class="fa fa-plus" aria-hidden="true"> Add New</i></button>}
                    @if (ViewContext.Controller.HasPermission("ToolListSetting-Edit"))
                    {
        <button class="btn btn-primary" id="onselectedit"><i class="fa fa-pencil-square-o" aria-hidden="true"> Edit</i></button>}
                    @if (ViewContext.Controller.HasPermission("ToolListSetting-Delete"))
                    {
        <button class="btn btn-danger" id="onselectdelete"><i class="fa fa-trash-o" aria-hidden="true"> Delete</i></button>}
                </div>
                <h2 class="panel-title" style="color:white;">List of Tools  </h2>


                <div style="float: right;padding-top: 2%;color:black; ">
                    <p>*Select record to edit/delete</p>
                    <p>*Use Search to filter tools id/any other text</p>
                </div>
            </header>
            <div class="panel-body">
                <div class="form-group">
                    <div class="panel-body">
                        @if (ViewContext.Controller.HasPermission("ToolListSetting-View"))
                        {
            <table class="table table-bordered table-striped mb-none" id="datatable-default" data-swf-path="assets1/vendor/jquery-datatables/extras/TableTools/swf/copy_csv_xls_pdf.swf">
                <thead style="background-color:teal;color:white;">
                    <tr>
                        <th class="centercls"> S.No </th>
                        <th style="display:none;"></th>
                        <th>Function</th>
                        <th>Machine</th>
                        <th> Tool ID </th>
                        <th> Tool Name</th>
                        <th> Life Cycle</th>
                        <th> Recommendation</th>
                    </tr>
                </thead>
                <tbody>
                    @{int RowNo = 0;}
                    @foreach (var item in Model)
                    {
    <tr class="gradeX">
        <td>
            @{RowNo++;}@RowNo
        </td>
        <td class="ID" style="display:none;">
            <span>@Html.Encode(item.ID)</span>
        </td>
        <td>
            <span>@Html.Encode(item.FunctionName)</span>
        </td>
        <td>
            <span>@Html.Encode(item.AssetName)</span>
        </td>
        <td class="ToolID">
            <span>@Html.Encode(item.ToolID)</span>
        </td>
        <td>
            <span>@Html.Encode(item.ToolName)</span>
        </td>
        <td class="RLC">
            <span>@Html.Encode(item.RatedLifeCycle)</span>
        </td>
        <td>
            <span>@Html.Encode(item.RecommendationText)</span>
        </td>
    </tr>}
                </tbody>
            </table> }
                        else
                        {
            <h2>View permission is denied</h2>}
                    </div>
                </div>
            </div>
        </section>
    </div>
</div>

<!--Edit Loss Data Details-->
<div class="row" id="divedit" style="display:none;">
    <div class="col-md-12">
        <form class="form" action="@Url.Action("Update_Toollist","UserSettings")" method="post">
            <section class="panel">
                <header class="panel-heading" style="background-color:teal;color:white;">
                    <div class="panel-actions">
                    </div>
                    <h2 class="panel-title" style="color:white;">Update Tool</h2>
                </header>
                <div class="panel-body">
                    <input type="hidden" id="eID" name="ID" />
                    <div class="form-group">
                        @*<label class="col-sm-2 control-label"> Line Code <span class="required">*</span></label>
                            <div class="col-sm-4">
                                <select class="form-control Line" name="Line_code" id="eLine" title="Select Line" required></select>
                            </div>*@
                        <label class="col-sm-2 control-label">Machine Code <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <select class="form-control Machine" name="Machine_Code" id="eMachine" title="Select Machine" required></select>
                        </div>
                        <label class="col-sm-2 control-label"> Sub Assembly @*<span class="required">*</span>*@</label>
                        <div class="col-sm-4">
                            <select class="form-control eSubassembly" name="Subassembly" id="eSubassembly" title="Select Sub Assembly">
                                <option value="">Select Machine</option>
                            </select>
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label"> Tool ID <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="eToolID" name="ToolID" class="form-control" title="Enter ID" readonly placeholder="Tool ID" required />
                        </div>
                        <label class="col-sm-2 control-label"> Tool Name <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="eToolName" name="ToolName" class="form-control" title="Enter Name" placeholder="Name" required />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label"> Make <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="eMake" name="Make" class="form-control" title="Enter Make" placeholder="Make" required />
                        </div>
                        <label class="col-sm-2 control-label"> UOM <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="eUOM" name="UOM" class="form-control" title="Enter UOM" placeholder="UOM" required />
                        </div>
                    </div>
                    <div class="form-group">

                        <label class="col-sm-2 control-label"> Rated Life Cycle <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="number" id="eRatedLifeCycle" name="RatedLifeCycle" min="0" oninput="validity.valid||(value='');" class="form-control" title="Enter RatedLifeCycle" min="0" step="any" placeholder="Rated Life Cycle" required />
                        </div>
                        <label class="col-sm-2 control-label"> Part Number <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="ePartNo" name="Part_number" class="form-control" title="Enter Part Number" placeholder="Part Number" required />
                        </div>
                    </div>
                    <div class="form-group">
                        <label class="col-sm-2 control-label">Recommendation Text <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="eRectext" name="Rectext" class="form-control" title="Enter Part Number" placeholder="Part Number" required readonly />
                        </div>

                        <label class="col-sm-2 control-label"> Conversion Parameter <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="eConversion_parameter" onkeypress="return onlyNumberKey(event)" name="Conversion_parameter" class="form-control" title="Enter Parameter" placeholder="Parameter" required />
                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-sm-2 control-label"> Serial Number <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="eSerialNo" name="SerialNo" class="form-control" title="Enter Serial No" placeholder="Serial No" required />
                        </div>
                        <label class="col-sm-2 control-label"> Classification <span class="required">*</span></label>
                        <div class="col-sm-4">
                            <input type="text" id="eClassification" name="Classification" class="form-control" title="Enter Classification" placeholder="Classification" required />
                        </div>
                    </div>



                </div>
                <footer class="panel-footer">
                    <div class="row">
                        <div class="col-sm-6 col-sm-offset-3" style="text-align:center;">
                            <button type="submit" class="btn btn-primary">Update</button>
                            <button type="reset" id="cancel" class="btn btn-danger cancel">Cancel</button>
                        </div>
                    </div>
                </footer>
            </section>
        </form>
    </div>
</div>

@Scripts.Render("~/bundles/jquery")
@Scripts.Render("~/bundles/jqueryval")
<script src="~/assets1/javascripts/swal.js"></script>
<script src="~/assets1/javascripts/swal_alert.js"></script>

@*<script type="text/javascript">
        $(document).ready(function () {
            $("#datatable-default").DataTable(
                {
                            "autoWidth": false,
                              "bLengthChange": false,
                             "lengthMenu": [[5], [5]]
                }
            );
        });
    </script>*@


@if (@TempData["message"] != null)
{
<script type="text/javascript">
        $(function () {
            console.log('@TempData["message"]');
        var msg = '@TempData["message"]';
        if (msg == "Inserted") {
            swal({
                icon: "success",
                title: "Details Added Successfully...!",
                button: true,
                timer: 4500
            })
        }
        else if (msg == "Updated") {
            swal({
                icon: "success",
                title: "Details Updated Successfully...!",
                button: true,
                timer: 4500
            })
        }
         else if (msg == "Already ToolID") {
            swal({
                icon: "error",
                title: "Failed to Add.Tool ID already exists",
                button: true,
                timer: 4500
            })
        }
        else if (msg == "Already ToolName") {
            swal({
                icon: "error",
                title: "Failed to Add.Tool Name already exists",
                button: true,
                timer: 4500
            })
        }
        else {
            swal({
                icon: "error",
                title: "Failed to Add",
                button: true,
                timer: 4500
            })
        }
    });
</script>}
<script type="text/javascript">
    $("#add").click(function () {
        $("#divnew").show();
        $("#divdetails").hide();
        $("#divedit").hide();
    });

    $(".cancel").click(function () {
        $("#divnew").hide();
        $("#divdetails").show();
        $("#divedit").hide();
    });
</script>
<script type="text/javascript">

    function onlyNumberKey(evt) {

        // Only ASCII character in that range allowed
        var ASCIICode = (evt.which) ? evt.which : evt.keyCode
        if (ASCIICode > 31 && (ASCIICode < 48 || ASCIICode > 57))
            return false;
        return true;
    }
    @*$(function () {
        var URL = '@System.Configuration.ConfigurationManager.AppSettings["url"]';
        var myDatas = {
            "QueryType": "Function",
            "Parameter": '@Session["PlantCode"]',
            "Parameter1": '@Session["CompanyCode"]'
        };
         var R_url = '@Url.Action("Login", "Main")';
            var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';
             //$.ajax({
             //    type: 'Get',
             //    url: URL + 'api/UserSettings/GetEmployee',
             //    headers: {
             //        Authorization: 'Bearer ' + user1
             //    },
             //    dataType: 'json'
             //}).success(function (response) {
        $.ajax({
            type: "POST",
            data: myDatas,
             headers: {
                     Authorization: 'Bearer ' + user1
                 },
            url: URL + 'api/UserSettings/User_settings_details',
            dataType: "json",
            success: function (response) {
                var s = '<option value="">Please Select</option>';
                for (var i = 0; i < response.data.Table.length; i++) {
                    s += '<option value="' + response.data.Table[i].FunctionID + '">' + response.data.Table[i].FunctionName + '</option>';
                }
                $(".Line").html(s);
            }
        }).error(function (response) {
            if (response.status == "401") {
                swal({
                    icon: "warning",
                    title: "Session Timeout",
                    button: true,
                    closeModal: false
                })
                window.location = R_url;
            }
            else {
                swal({
                    icon: "warning",
                    title: response.responseText,
                    button: true,
                    closeModal: false
                })

            }
             });

    });*@
</script>

<script type="text/javascript">
    //Function to select row
    $("#datatable-default tbody tr").click(function () {
        var bl;
        var editval;
        if ($(this).hasClass('selected')) {
            bl = "row selected";
            $(this).removeClass('selected');
        }
        else {
            bl = "row unselected";
            $(this).addClass('selected').siblings().removeClass('selected');
            editval = $(this).find('td:first').html();
            //alert(editval)
        }
    });
</script>
<!--Style section for selection of rows-->
<style>
    #datatable-default tbody tr:not(.table-header) {
        cursor: pointer;
    }

    #datatable-default tbody .selected:not(.table-header) {
        background-color: brown;
        color: #FFF;
    }
</style>
<script type="text/javascript">
     var URL = '@System.Configuration.ConfigurationManager.AppSettings["url"]';
    //edit function on selction of row

    $("#onselectedit").on("click", function () {
        //function to retrieve id on select to edit the row
        $("#datatable-default tbody tr").each(function () {
            if ($(this).hasClass('selected')) {

                var row = $(this);
                var ID = row.find(".ID span").html();
                var Ratedlifecycle = row.find(".RLC span").html();
                var ToolID = row.find(".ToolID span").html();
                //alert(CompanyCode)
                        var myData = {
                    "QueryType": "Edit_tool",
                            "Parameter": ID,
                    "CompanyCode": '@Session["CompanyCode"]',
                            "PlantCode": '@Session["PlantCode"]',
                            "LineCode": '@Session["LineCode"]',
                            "Parameter1": ToolID,
                            "Parameter2": Ratedlifecycle
                };

                $("#divedit").show();
                $("#divnew").hide();
                $("#divdetails").hide();
                  var R_url = '@Url.Action("Login", "Main")';
            var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';
             //$.ajax({
             //    type: 'Get',
             //    url: URL + 'api/UserSettings/GetEmployee',
             //    headers: {
             //        Authorization: 'Bearer ' + user1
             //    },
             //    dataType: 'json'
             //}).success(function (response) {
                $.ajax({
                    type: "POST",
                    url: URL + 'api/Values/User_settings_details',
                    data: myData,
                      headers: {
                     Authorization: 'Bearer ' + user1
                 },
                    dataType: "json",
                    beforeSend: function () {
                        $('.loading').show();
                    },
                    complete: function () {
                        $('.loading').hide();
                    },
                    success: function (response) {

                        $('#eID').val(response.data.Table[0].ID);
                        $('#eToolID').val(response.data.Table[0].ToolID);
                        $('#eToolName').val(response.data.Table[0].ToolName);
                        $('#eMake').val(response.data.Table[0].Make);
                        $('#eUOM').val(response.data.Table[0].UOM);
                        $('#ePartNo').val(response.data.Table[0].Part_number);
                        $('#eClassification').val(response.data.Table[0].Classification);
                        $('#eRatedLifeCycle').val(response.data.Table[0].RatedLifeCycle);
                        $('#eSerialNo').val(response.data.Table[0].SerialNo);
                        $('#eRectext').val(response.data.Table[0].RecommendationText);

                        $('#eConversion_parameter').val(response.data.Table[0].Conversion_parameter);

                        var linecode = '@Session["LineCode"]';

                        @*var myDatas = {
                            "QueryType": "Function",
                            "Parameter": '@Session["PlantCode"]',
                            "Parameter1": '@Session["CompanyCode"]'
                        };

                        $.ajax({
                            type: "POST",
                            data: myDatas,
                             headers: {
                                 Authorization: 'Bearer ' + user1
                             },
                            url: URL + 'api/UserSettings/User_settings_details',
                            dataType: "json",
                            success: function (response1) {
                                var s = '<option value="" disabled>Please Select</option>';
                                for (var i = 0; i < response1.data.Table.length; i++) {
                                    if (response.data.Table[0].Line_code == response1.data.Table[i].FunctionID) {
                                        s += '<option value="' + response1.data.Table[i].FunctionID + '" selected=selected>' + response1.data.Table[i].FunctionName + '</option>';
                                    }
                                    else {
                                        s += '<option value="' + response1.data.Table[i].FunctionID + '">' + response1.data.Table[i].FunctionName + '</option>';
                                    }
                                }
                                $("#eLine").html(s);
                            }
                        }).error(function (response) {
                            if (response.status == "401") {
                                swal({
                                    icon: "warning",
                                    title: "Session Timeout",
                                    button: true,
                                    closeModal: false
                                })
                                window.location = R_url;
                            }
                            else {
                                swal({
                                    icon: "warning",
                                    title: response.responseText,
                                    button: true,
                                    closeModal: false
                                })

                            }
                             });*@

                        var myDatas = {
                            "QueryType": "Assets",
                            "Parameter": '@Session["PlantCode"]',
                            "Parameter1": '@Session["CompanyCode"]',
                            "Parameter2": linecode,
                            "CompanyCode": '@Session["CompanyCode"]',
                            "PlantCode": '@Session["PlantCode"]',
                            "LineCode": '@Session["LineCode"]'

                        };

                        $.ajax({
                            type: "POST",
                            data: myDatas,
                             headers: {
                                 Authorization: 'Bearer ' + user1
                             },
                            url: URL + 'api/Values/User_settings_details',
                            dataType: "json",
                            success: function (response2) {
                                var s = '<option value="" disabled>Please Select</option>';
                                for (var i = 0; i < response2.data.Table.length; i++) {
                                    if (response2.data.Table[i].FunctionName == response.data.Table[0].Line_code) {
                                        s += '<option value="' + response2.data.Table[i].AssetID + '" selected=selected>' + response2.data.Table[i].AssetName + '</option>';
                                    }
                                    else {
                                        s += '<option value="' + response2.data.Table[i].AssetID + '">' + response2.data.Table[i].AssetName + '</option>';
                                    }
                                }
                                $("#eMachine").html(s);
                            }
                        }).error(function (response) {
                            if (response.status == "401") {
                                swal({
                                    icon: "warning",
                                    title: "Session Timeout",
                                    button: true,
                                    closeModal: false
                                })
                                window.location = R_url;
                            }
                            else {
                                swal({
                                    icon: "warning",
                                    title: response.responseText,
                                    button: true,
                                    closeModal: false
                                })

                            }
                             });
                    }
                    , error: function (response) {

                    }
                }).error(function (response) {
                    if (response.status == "401") {
                        swal({
                            icon: "warning",
                            title: "Session Timeout",
                            button: true,
                            closeModal: false
                        })
                        window.location = R_url;
                    }
                    else {
                        swal({
                            icon: "warning",
                            title: response.responseText,
                            button: true,
                            closeModal: false
                        })

                    }
             });
                return false;
            }
            else {
                //alert("Please select a record to proceed...")
                return;
            }

        });
    });
    //delete function on selection of row
    
     $("#onselectdelete").on("click", function () {
        //function to retrieve id on select to delete the row
        var CompanyCode;
        $("#datatable-default tbody tr").each(function () {
            if ($(this).hasClass('selected')) {

                    if (confirm("Do you want to Delete this...?")) {
                        var row = $(this);
                        var ID = row.find(".ID span").html();
                        var Ratedlifecycle = row.find(".RLC span").html();
                        var myData = {
                            "QueryType": "Delete_tool",
                            "Parameter": ID,
                            "Parameter1": Ratedlifecycle,
                            "CompanyCode": '@Session["CompanyCode"]',
                         "PlantCode": '@Session["PlantCode"]',
                         "LineCode": '@Session["LineCode"]'
                        };
                         var R_url = '@Url.Action("Login", "Main")';
            var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';
             //$.ajax({
             //    type: 'Get',
             //    url: URL + 'api/UserSettings/GetEmployee',
             //    headers: {
             //        Authorization: 'Bearer ' + user1
             //    },
             //    dataType: 'json'
             //}).success(function (response) {
                        $.ajax({
                            type: "POST",
                            url: URL + 'api/Values/delete_User_settings_details',
                            data: myData,
                             headers: {
                                 Authorization: 'Bearer ' + user1
                             },
                            dataType: "json",
                            beforeSend: function () {
                                $('.loading').show();
                            },
                            complete: function () {
                                $('.loading').hide();
                            },
                            success: function (response) {
                                swal({
                                    icon: "success",
                                    title: "Details Deleted Successfully...!",
                                    button: true,
                                    timer: 4500
                                })
                               window.location = '@Url.Action("Tool_list", "UserSettings")';
                            }
                        }).error(function (response) {
                            if (response.status == "401") {
                                swal({
                                    icon: "warning",
                                    title: "Session Timeout",
                                    button: true,
                                    closeModal: false
                                })
                                window.location = R_url;
                            }
                            else {
                                swal({
                                    icon: "warning",
                                    title: response.responseText,
                                    button: true,
                                    closeModal: false
                                })

                            }
             });
                    }

                return false;
            }
            else {
                //alert("Please select a record to proceed...")
                return;
            }

        });
    });

</script>
<script>
    $(document).ready(function () {
        $('.updated_time').text("N/A");
        $('.shift').text("N/A");

        if ($.fn.DataTable.isDataTable('#datatable-default')) {
            $('#datatable-default').DataTable().destroy();
        }


        $('#datatable-default').DataTable({

            "responsive": true,
            "autoWidth": false,
            scrollY: true,
            scrollX: true,
            scroller: true
        });


    });
</script>

<script type="text/javascript">
    $(function() {
        //alert(this.value);

          var myData = {
                        "Flag": "Subsystem",
                        "CompanyCode": '@Session["CompanyCode"]',
                        "PlantCode": '@Session["PlantCode"]',
                        "LineCode":'@Session["LineCode"]',
                    };
                    var R_url = '@Url.Action("Login", "Main")';
                   var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';

                   $.ajax({
                        type: "POST",
                       url: '/Main/GetSettingdatas1',
                        data: myData,
                        dataType: "json",
                        success: function (response) {
                            var s = '<option value="">Select Machine</option>';
                            for (var i = 0; i < response.data.length; i++) {
                                s += '<option value="' + response.data[i].Code + '">' + response.data[i].Name + '</option>';
                            }
                            $(".Machine").html(s);
                        },
                        error: function (response) {
                            if (response.status == "401") {
                                swal({
                                    icon: "warning",
                                    title: "Session Timeout",
                                    button: true,
                                    closeModal: false
                                })
                                window.location = R_url;
                            }
                            else {
                                swal({
                                    icon: "warning",
                                    title: response.responseText,
                                    button: true,
                                    closeModal: false
                                })

                            }
                        }
                    });

    });

</script>

<script type="text/javascript">
    $(function() {
        //alert(this.value);

          var myData = {
                        "Flag": "Subsystem",
                        "CompanyCode": '@Session["CompanyCode"]',
                        "PlantCode": '@Session["PlantCode"]',
                        "LineCode":'@Session["LineCode"]',
                    };
                    var R_url = '@Url.Action("Login", "Main")';
                   var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';

                   $.ajax({
                        type: "POST",
                       url: '/Main/GetSettingdatas1',
                        data: myData,
                        dataType: "json",
                        success: function (response) {
                            var s = '<option value="">Select Machine</option>';
                            for (var i = 0; i < response.data.length; i++) {
                                s += '<option value="' + response.data[i].Code + '">' + response.data[i].Name + '</option>';
                            }
                            $("#eMachine").html(s);
                        },
                        error: function (response) {
                            if (response.status == "401") {
                                swal({
                                    icon: "warning",
                                    title: "Session Timeout",
                                    button: true,
                                    closeModal: false
                                })
                                window.location = R_url;
                            }
                            else {
                                swal({
                                    icon: "warning",
                                    title: response.responseText,
                                    button: true,
                                    closeModal: false
                                })

                            }
                        }
                    });

    });

</script>


<script>
     $("#Machine").change(function () {
             var Machine = $('#Machine').val();
             var data = {
                 "QueryType": "Get_subassambly",
                 "Parameter": '@Session["PlantCode"]',
                 "Parameter1": '@Session["CompanyCode"]',
                 "Parameter2": Machine,
                  "CompanyCode": '@Session["CompanyCode"]',
                    "PlantCode": '@Session["PlantCode"]',
                    "LineCode": '@Session["LineCode"]'
         };

            var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';
             $.ajax({
                 type: "POST",
                 data: data,
                 headers: {
                     Authorization: 'Bearer ' + user1
                 },
                 url: URL + "api/Values/User_settings_details",
                 dataType: "json",

                 success: function (response) {

                     if (response.data.Table.length > 0) {
                         var s = '<option value="">Select</option>';
                         console.log(response)
                         for (var i = 0; i < response.data.Table.length; i++) {

                             s += '<option value="' + response.data.Table[i].SubassemblyID + '">' + response.data.Table[i].SubassemblyName + '</option>';
                         }
                         $("#Subassembly").html(s);
                     }
                     else {
                         var s = '<option value="null">No Subassembly</option>';

                         $("#Subassembly").html(s);
                     }

                 }
             })
     });


     $("#eMachine").change(function () {
             var Machine = $('#eMachine').val();
             var data = {
                 "QueryType": "Get_subassambly",
                 "Parameter": '@Session["PlantCode"]',
                 "Parameter1": '@Session["CompanyCode"]',
                 "Parameter2": Machine,
                  "CompanyCode": '@Session["CompanyCode"]',
                    "PlantCode": '@Session["PlantCode"]',
                    "LineCode": '@Session["LineCode"]'
             };
            var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';
             $.ajax({
                 type: "POST",
                 data: data,
                 headers: {
                     Authorization: 'Bearer ' + user1
                 },
                 url: URL + "api/UserSettings/User_settings_details",
                 dataType: "json",

                 success: function (response) {
                     if (response.length > 0) {
                         var s = '<option value="">Select</option>';
                         for (var i = 0; i < response.data.Table.length; i++) {
                             if (response.data.Table[i].SubassemblyID == response.data.Table[0].SubassemblyID) {
                                 s += '<option value="' + response.data.Table[i].SubassemblyID + '" selected=selected>' + response.data.Table[i].SubassemblyName + '</option>';
                             }
                             else {
                                 s += '<option value="' + response.data.Table[i].SubassemblyID + '">' + response.data.Table[i].SubassemblyName + '</option>';
                             }
                         }

                         $("#eSubassembly").html(s);
                     }
                     else {
                         var s = '<option value="null">No Subassembly</option>';

                         $("#eSubassembly").html(s);
                     }

                 }
             })


         });

    var maxv = 0;

    $(".addCF").click(function () {
        if (maxv <= 20) {
            $("#customFields").append('<tr valign="top"><th scope="row"><label for="customFieldName"></label></th><td><input type="number" class="code" id="RatedLifeCycle" min="0" onkeypress="return event.charCode >= 48" name="RatedLifeCycle[]" placeholder="Input RatedLifeCycle" /> &nbsp; &nbsp; &nbsp; &nbsp; <input type="text" class="code" id="Rectext" name="Rectext[]" value="" placeholder="Input Rectext" /> &nbsp; <a href="javascript:void(0);" class="remCF">Remove</a></td></tr>');
            maxv++;
        }
        else {
            return alert("max no of cycle reached")
        }
        });
    $("#customFields").on('click', '.remCF', function () {
        $(this).parent().parent().remove();
    });



    $(".submit").click(function (event) {

        var rectext = new Array();
            var permission = new Array();
        var role = new Array();


        var roles = {};
        let list = [];
        roles.ToolID = $('#ToolID').val();
        roles.ToolName = $('#ToolName').val();
        roles.Machine_Code = $('#Machine').val();
        roles.Subassembly = $('#Subassembly').val();
        roles.Part_number = $('#PartNo').val();
        roles.Conversion_parameter = $('#Conversion_parameter').val();
        roles.Make = $('#Make').val();
        roles.UOM = $('#UOM').val();
        roles.SerialNo = $('#SerialNo').val();

        roles.Classification = $('#Classification').val();

        var tool = $('#ToolID').val();

        var ToolName = $('#ToolName').val();
        var PartNo = $('#PartNo').val();
        var SerialNo = $('#SerialNo').val();
        var Conversion_parameter = $('#Conversion_parameter').val();
        var SerialNo = $('#Make').val();
        var UOM = $('#UOM').val();
        var Classification = $('#Classification').val();



        var Name = sanitizeisrepl(ToolName);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => Name.includes(el));

        if (test1 == true) {
            //upva = 1;

            //alert("special charecters are not allowed");
            return alert("special charecters are not allowed");


        }
        var ID = sanitizeisrepl(tool);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => ID.includes(el));

        if (test1 == true) {
            //upva = 1;

            return alert("special charecters are not allowed");

        }
        var pn = sanitizeisrepl(PartNo);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => pn.includes(el));

        if (test1 == true) {
            //upva = 1;

            return alert("special charecters are not allowed");

        }

        var sn = sanitizeisrepl(SerialNo);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => sn.includes(el));

        if (test1 == true) {
            //upva = 1;

            return alert("special charecters are not allowed");

        }
        var cp = sanitizeisrepl(Conversion_parameter);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => cp.includes(el));

        if (test1 == true) {
            //upva = 1;

            return alert("special charecters are not allowed");

        }

        var uom = sanitizeisrepl(UOM);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => uom.includes(el));

        if (test1 == true) {
            //upva = 1;

            return alert("special charecters are not allowed");

        }
        var classifi = sanitizeisrepl(Classification);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => classifi.includes(el));

        if (test1 == true) {
            //upva = 1;

            return alert("special charecters are not allowed");

        }

        var inputs = document.getElementsByName("RatedLifeCycle[]");
        var admissionNumber = "";
        for (var j = 0; j < inputs.length; j++) {

            admissionNumber = inputs[j].value;
            if (admissionNumber == '') {


                return alert("Rated life cycle field is empty");
            }
        }

        var inputs1 = document.getElementsByName("Rectext[]");
        var admissionNumber1 = "";
        for (var j = 0; j < inputs1.length; j++) {

            admissionNumber1 = inputs1[j].value;
            if (admissionNumber1 == '') {
                return alert("Recommendation field is empty");

            }
        }


           // var checked = $("input[type=checkbox]:checked").length;

            //roles.Line_Code = $('.lineecode').val();
            //var remval = sanitize(roles.RoleName);
            //var remval1 = sanitize(roles.RoleDescription);
            //var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
            //var test1 = conditions.some(el => remval.includes(el));
            //var test2 = conditions.some(el => remval1.includes(el));

            //else if (roles.Line_Code == "") {
            //    swal({
            //        icon: "error",
            //        title: "Please Select Line...!",
            //        button: true,
            //        timer: 4500
            //    })
            //    return;
            //}
            if ($('.submit').text() == "Update") {
                roles.QueryType = "Update";

            }
            else {
                roles.QueryType = "Insert";

            }
              //var a = $('#RoleID').val();
               // alert(a)
            roles.ToolID = $('#ToolID').val();
            role.push(roles);

            //permissions.RatedLifeCycle = $('#RatedLifeCycle').val();
            //permissions.RecommendationText = $('#Rectext').val();

            //alert(permissions.RatedLifeCycle)




        var input = document.getElementsByName('RatedLifeCycle[]');
        var rt = document.getElementsByName('Rectext[]');
        var k = "The respective values are :";
        for (var i = 0; i < input.length; i++) {
            var permissions = {};

            var a = input[i];
            var rta = rt[i];

            permissions.RatedLifeCycle = a.value
            permissions.ToolID = $('#ToolID').val();
            permissions.ToolName = $('#ToolName').val();
            permissions.Machine_Code = $('#Machine').val();
            permissions.Subassembly = $('#Subassembly').val();
            permissions.SerialNo = $('#SerialNo').val();
            permissions.Part_number = $('#PartNo').val();
            permissions.Rectext = rta.value
            permission.push(permissions);

        }
        if ($('.submit').text() == "Update") {

            permissions.QueryType = "Update";
        }
        else {

            permissions.QueryType = "Insert";
        }
        //var rt = document.getElementsByName('Rectext[]');
        //var rtk = "The respective values are :";
        //for (var i = 0; i < rt.length; i++) {
        //    var rectexts = {};
        //    var rta = rt[i];
        //   rtk =rtk + "array[" + i + "].value= "
        //        + rta.value + " ";


        //    rectexts.Rectext = rta.value
        //    rectexts.ToolID = $('#ToolID').val();

        //    rectext.push(rectexts);


        //}

        //permission.push(list);


             var R_url = '@Url.Action("Login", "Main")';
            var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';

             //$.ajax({
             //    type: 'Get',
             //    url: URL + 'api/UserSettings/GetEmployee',
             //    headers: {
             //        Authorization: 'Bearer ' + user1
             //    },
             //    dataType: 'json'
             //}).success(function (response) {
            $.ajax({
                type: "POST",
                url: "/UserSettings/Add_Tools",
                data: '{permissions: ' + JSON.stringify(permission) + ',role: ' + JSON.stringify(role) + '}',
                 //headers: {
                 //    Authorization: 'Bearer ' + user1
                 //},
                contentType: "application/json; charset=utf-8",
                dataType: "json",
                beforeSend: function () {
                    $('.loading').show();
                },
                complete: function () {
                    $('.loading').hide();
                },
                success: function (response) {

                    //if ($('.submit').text() == "Update") {
                    //    swal({
                    //        icon: "success",
                    //        title: "Details Updated Successfully...!",
                    //        button: true,
                    //        timer: 4500
                    //    })
                    //}
                    //else if ($('.submit').text() == "Submit") {
                    //    swal({
                    //        icon: "success",
                    //        title: "Details Added Successfully...!",
                    //        button: true,
                    //        timer: 4500
                    //    })
                    //}
                    $('.submit').text('Submit');
                    window.location = '@Url.Action("Tool_list", "UserSettings")';
                }
            })
             //   .error(function (response) {
             //    swal({
             //        icon: "warning",
             //        title: "Session Expired",
             //        button: true,
             //        closeModal: false
             //    })
             //    window.location = R_url;
             //});

        });


    function checktool() {
        var tool = $('#ToolID').val();


             var data = {
                 "QueryType": "Get_tool",
                 "Parameter1":'@Session["LineCode"]',
                 "Parameter": tool,
                  "CompanyCode": '@Session["CompanyCode"]',
                    "PlantCode": '@Session["PlantCode"]',
                    "LineCode": '@Session["LineCode"]'
            };

            var user1 = '@Session["Token"]' + ':' + '@Session["UserName"]';
             $.ajax({
                 type: "POST",
                 data: data,
                 headers: {
                     Authorization: 'Bearer ' + user1
                 },
                 url: URL + "api/Values/User_settings_details",

                 dataType: "json",

                 success: function (response) {
                     if (response.data.Table.length > 0) {


                         $('#ToolName').val(response.data.Table[0].ToolName);
                         $('#Make').val(response.data.Table[0].Make);
                         $('#UOM').val(response.data.Table[0].UOM);
                         $('#PartNo').val(response.data.Table[0].Part_number);
                         $('#Classification').val(response.data.Table[0].Classification);
                         $('#SerialNo').val(response.data.Table[0].SerialNo);
                         $('#Conversion_parameter').val(response.data.Table[0].Conversion_parameter);

                     }
                     else {

                     }

                 }
             })

    }


    function sanitizeisrepl(isrepval) {

        const map = {
            '&': '&amp;',
            '<': '&lt;',
            '>': '&gt;',
            '"': '&quot;',
            "'": '&#x27;',
            "/": '&#x2F;',
        };
        const reg = /[&<>"'/]/ig;
        return isrepval.replace(reg, (match) => (map[match]));
    }

    document.form1.addEventListener("submit", function (event) {

        //document.getElementById("submit1").addEventListener("click", function () {
        //document.getElementById("#submit1").addEventListener("submit", function () {
        //document.form1.addEventListener("#submit1", function (event) {

        var tool = $('#ToolID').val();

        var ToolName = $('#ToolName').val();
        var PartNo = $('#PartNo').val();
        var SerialNo = $('#SerialNo').val();
        var Conversion_parameter = $('#Conversion_parameter').val();
        var SerialNo = $('#Make').val();
        var UOM = $('#UOM').val();
        var Classification = $('#Classification').val();


        var Name = sanitizeisrepl(ToolName);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => Name.includes(el));

        if (test1 == true) {
            //upva = 1;

            //alert("special charecters are not allowed");
            event.preventDefault();

        }
        var ID = sanitizeisrepl(tool);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => ID.includes(el));

        if (test1 == true) {
            //upva = 1;

            //alert("special charecters are not allowed");
            event.preventDefault();

        }
        var pn = sanitizeisrepl(PartNo);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => pn.includes(el));

        if (test1 == true) {
            //upva = 1;

            //alert("special charecters are not allowed");
            event.preventDefault();

        }

        var sn = sanitizeisrepl(SerialNo);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => sn.includes(el));

        if (test1 == true) {
            //upva = 1;

            //alert("special charecters are not allowed");
            event.preventDefault();

        }
        var cp = sanitizeisrepl(Conversion_parameter);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => cp.includes(el));

        if (test1 == true) {
            //upva = 1;

            //alert("special charecters are not allowed");
            event.preventDefault();

        }

        var uom = sanitizeisrepl(UOM);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => uom.includes(el));

        if (test1 == true) {
            //upva = 1;

            //alert("special charecters are not allowed");
            event.preventDefault();

        }
        var classifi = sanitizeisrepl(Classification);

        var conditions = ["&lt;", "&#x2F;", "&#x27;", "&amp;", "&gt;", "&quot;"];
        var test1 = conditions.some(el => classifi.includes(el));

        if (test1 == true) {
            //upva = 1;

            //alert("special charecters are not allowed");
            event.preventDefault();

        }

        var inputs = document.getElementsByName("RatedLifeCycle[]");
        var admissionNumber = "";
        for (var j = 0; j < inputs.length; j++) {

            admissionNumber = inputs[j].value;
            if (admissionNumber == '') {
                //alert("Rated life cycle field is empty");
                event.preventDefault();
            }
        }

        var inputs1 = document.getElementsByName("Rectext[]");
        var admissionNumber1 = "";
        for (var j = 0; j < inputs1.length; j++) {

            admissionNumber1 = inputs1[j].value;
            if (admissionNumber1 == '') {
                //alert("Recommendation field is empty");
                event.preventDefault();
            }
        }


    });
</script>

